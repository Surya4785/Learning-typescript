// enums

enum UserRole {
    ADMIN = "admin",
    USER = "user",
    SUPER_ADMIN = "super_admin"
}

enum StatusCode {
    SUCCESS = 200,
    NOT_FOUND = "not found  status code 404",
    INTERNAL_ERROR = "abandoned status code 500"
}